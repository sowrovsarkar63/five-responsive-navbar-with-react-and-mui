import { Button } from "@mui/material";
import React from "react";

const Experiments = () => {
    return (
        <div>
            <h2>Experimentals</h2>
            {/* mui button  */}
            <Button variant="contained">Experimental button </Button>
        </div>
    );
};

export default Experiments;
